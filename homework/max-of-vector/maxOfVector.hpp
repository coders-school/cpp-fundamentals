#pragma once
#include <vector>
#include <limits>

int maxOfVector(const std::vector<int>& vec) {
    int maxV = max_element(vec);
    for (int  : vec) {
        if(n > naj) maxV = n;
    }
    return maxV;
}
